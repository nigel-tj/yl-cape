.row
  .col-sm-6.col-md-4.col-md-offset-4
    - @events.each do |event|
      .thumbnail
        
        = image_tag event.image_link
        .caption
          = link_to "Vip tickets" , user_event_vip_tickets_path(@event)
          = link_to "standard_tickets" , user_event_standard_tickets_path(@event)

          %br

